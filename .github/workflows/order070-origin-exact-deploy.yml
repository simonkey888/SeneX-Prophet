name: ORDER070 R6 Terminal Caller
on:
  push:
    branches: [ops/senex-zero-cost-audit-20260809]
    paths: [.github/workflows/order070-origin-exact-deploy.yml]
permissions:
  contents: read
  statuses: write
  actions: read
jobs:
  terminal:
    uses: ./.github/workflows/order070-r6-terminal.yml
    secrets: inherit
# R6 trigger: pristine deploy_stability checkout; candidate d166495e unchanged.
