name: ARQ Phase D-E Superseded Workflow

on:
  workflow_call:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'SUPERSEDED_BY=arq-phase-d-e-manual-backup-deploy-v2.yml'
